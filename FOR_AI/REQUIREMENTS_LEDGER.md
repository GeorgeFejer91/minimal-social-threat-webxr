# Requirements ledger

This ledger consolidates the user’s requests in chronological order. “Implemented” describes software state, not scientific validation.

| ID | Demand | Status | Notes |
| --- | --- | --- | --- |
| R01 | Investigate scientifically validated minimal 2D emotion/avatar stimuli | Researched | Candidate literature and libraries are in the bibliography; the project-authored faces are not validated. |
| R02 | Represent social agents with minimal game-like features that still carry social cues | Implemented prototype | Procedural 2D and 3D agents carry expression, orientation, role, gait, gesture, and dialogue cues. |
| R03 | Surround the observer with a group; introduce an approaching threat | Superseded in spatial layout | The approach remains; R20 replaces the surrounding ring with a front-facing crowd. |
| R04 | Agents become avoidant and afraid as the threat comes nearer | Implemented | Detection, alert, startle, fear, flee, and freeze are explicit agent fields. |
| R05 | Optional VR/MR, predefined background, or passthrough | Implemented where browser supports it | `immersive-vr` and `immersive-ar`; virtual clearing or transparent compositor background. Physical-device verification remains required. |
| R06 | Browser top-down companion synchronized in realtime | Implemented, extended by R27 | Full scene snapshots and bounded commands; R27 adds a 3D state reconstruction and XR/runtime readback. |
| R07 | Reuse the Affect Tracker’s VDO.Ninja solution | Implemented adaptation | VDO.Ninja SDK 1.5.5, data-only room/channel, no media tracks; ordinary trial start or the dedicated headset route starts broadcast and the operator auto-discovers. |
| R08 | Publish through GitHub Pages and provide a launch URL | Existing deployment; update pending | Repository remote and live URL are recorded in the README. New changes must be pushed/deployed after validation. |
| R09 | Smartphone-compatible layout like the Affect Tracker | Implemented | Safe areas, touch targets, scroll-safe canvases, and a single-column mobile layout. |
| R10 | Complete 2D phone trial; VR only optional | Implemented | 2D Canvas is the primary trial. WebGL/WebXR loads lazily. |
| R11 | Investigate publicly available human assets | Researched, not bundled | Rocketbox is the leading behavior-ready candidate; VALID is the leading demographic-identity candidate. |
| R12 | Agents meander asymmetrically, face/look at one another, converse, and organize socially | Implemented prototype | Six dyads, seeded independent trajectories, gaze targets, alternating talk/listen roles, and social links. |
| R13 | Generate sounds and short dialogue cues in a binaural manner | Implemented prototype | Caption semantics plus project-authored non-lexical synthesis; one HRTF-positioned source per active agent cue. Fixed recorded speech is recommended for a final study. |
| R14 | Group behavior changes when the threatening cue menaces toward the observer | Implemented | Individual detection delays create non-simultaneous alarm transmission and flight. |
| R15 | Add a first-read “for AI” folder with project scope, goals, bibliography, and source assets | Implemented | Root `AGENTS.md` mandates this folder’s reading order. |
| R16 | Make the threat a stylized shrouded darkness with faint agentic cues such as red eyes | Implemented | Generic project-authored shadow; not a film-character replica. |
| R17 | Use a scientifically grounded threatening sound range and binaurally bind it to the approaching agent | Implemented with caveat; refined by R29 | The final three-second cue now reconstructs the reported Taffou et al. 500 Hz harmonic-complex/70 Hz full-depth modulation method and follows the live threat through HRTF direction, controlled relative distance level, and propagation delay. Approximate published upper-harmonic amplitudes and a different renderer/trajectory mean it is not the authors' original or an independently validated project stimulus. |
| R18 | Implement a working VR threat-approach scenario for social-neuroscience use | Working prototype | Automated software gates can pass; study readiness still requires acoustic calibration, headset tests, ethics/safety review, and pilot validation. |
| R19 | Remove the visible VR safety-limit marker and black warning sign; start the VR experiment with the right-controller A button | Implemented, later launch behavior superseded by R21 | The invisible 1.8 m constraint remains enforced. Right-hand `xr-standard` button index 4 restarts after completion or resumes a pause; R21 additionally makes the immersive launch click start/continue immediately. Either trigger remains pause-only. |
| R20 | Let dyads exchange friendly-valence tones; place a large crowd in front; keep the distant shadow initially invisible and increase its opacity during approach | Implemented prototype | Twelve agents form six front-facing dyads. Baseline cues use smooth, moderate-pitch, harmonic/consonant synthesis informed by affect research, with explicit cultural/validation caveats. The shadow starts at −16 m with zero participant-visible opacity and fades in monotonically with proximity. |
| R21 | Remove the readiness checkbox; expose direct Start 2D and Start immersive 3D controls; allow a running 2D trial to continue in XR | Implemented | The lazy 3D engine prewarms invisibly. The immersive button calls `requestSession` directly from its click and then starts/resumes the same trial clock. Right-controller A restarts/resumes and triggers pause. |
| R22 | Make synchronization easy from an external browser over VDO.Ninja | Implemented prototype, extended by R27 | Starting a normal participant trial starts the data broadcast without awaiting it before WebXR entry; the dedicated headset route advertises before trial start. The operator starts discovery when opened and retains retry/disconnect/source selection. |
| R23 | Add a dropdown-selectable publicly available human avatar | Implemented with caveat | The optional human crowd uses the bundled Cesium Man GLB (CC BY 4.0) in 3D and a human-proportioned project-authored 2D rendering. This asset is a format sample, not an emotion-validated social stimulus. |
| R24 | Add a spider as the third threat and make presence/audio more menacing | Implemented evidence-informed prototype; audio standardized by R29 | The runtime uses a project-authored, viewer-facing articulated spider with eight three-segment legs and a deterministic alternating-tetrapod gait. The earlier unrigged CC0 Huntsman GLB is retained only as a provenance/reference file because it contained a visible `Cube_2` prop and could not produce limb locomotion. Both shadow and spider fade in during approach. R29 replaces the exploratory spider-only clicks/inharmonic composite with the common layered threat protocol so threat identity is not silently confounded by a different audio construction. |
| R25 | Keep the repository as a GitHub project in the general `GithubVR` project collection | Implemented | The canonical local checkout is `D:\GithubVR\github-projects\minimal-social-threat-webxr`, with `origin` set to `https://github.com/GeorgeFejer91/minimal-social-threat-webxr.git`. |
| R26 | Replace flat face plates with a scientifically grounded minimal SVG emotion library, smooth feature morphs, and organic sphere wrapping | Implemented evidence-grounded prototype | `lib/facial-expression.ts` defines neutral plus six conventional component-emotion end states with shared cubic-Bézier topology, arbitrary pairwise/weighted morph functions, standalone planar/spherical SVG serialization, Canvas rendering, and explicit spherical projection. Generated SVG endpoints are committed under `public/assets/faces/`. Three.js tessellates the live SVG curves into sphere-conforming vector meshes, so procedural orb heads use neither a face plane nor a facial bitmap texture. End-state design is informed by FACS/recognition studies but these exact graphics and transitions require target-population validation. |
| R27 | Make the Quest threat-lab view work immersively and create a bidirectional PC↔headset control/observation bridge over VDO.Ninja | Implemented prototype; deployed-device acceptance remains required | `?view=headset` auto-starts a data-only host. Transport/commands use envelope v2 while scenario schema is v5. The PC operator gets authoritative top-down/3D state reconstruction, full scenario controls, XR request/exit, exact bounded receipts, and XR/runtime readback. An incoming `request-xr` is staged for a trusted local headset **Confirm** action because WebXR forbids silent network-initiated immersive entry. Immersive scenario time is driven by XR animation frames; optional `layers` is requested for compositor compatibility. No video, media, controller pose, or headset pose crosses the bridge. A targeted physical Quest smoke pass establishes only device/browser implementation behavior, never scientific validation. |
| R28 | Keep spatial/surround sound off by default in immersive VR without adding another preference | Superseded by R32 after wording clarification | This interpretation was implemented and device-tested, but the follow-up clarified that spatial sound itself should be the automatic immersive default rather than an extra setting. The Meta Home background stream diagnosed at the time was outside the application. |
| R29 | Borrow the PPS Kit binaural-generation approach and spatially incorporate scientifically grounded threat cues into the approaching-threat scenario | Implemented prototype; activation semantics superseded by R32 | Source/renderer separation is explicit. A 30 ms/10 ms-edge/95 ms-period PPS Kit-derived broadband burst train follows the threat throughout approach; the final three seconds add a 500–4000 Hz, full-depth 70 Hz roughness reconstruction. Browser HRTF direction follows an explicit 3D visual anchor (1.55 m upright; 0.42 m spider), while a single −18→0 dB relative-level ramp and distance/speed-of-sound delay follow proximity; schema v5 logs cue coordinates and exact protocol identity. IADS/IADS-E recordings are not bundled because their redistribution terms are insufficient. |
| R30 | Keep trees out of the threat route and replace the low-detail background trees with a higher-quality forest | Implemented prototype | A shared deterministic layout places 28 varied broadleaf/conifer trees around a ±2.4 m half-width central corridor; tests include each complete canopy radius rather than checking trunks alone. Canvas and WebXR add layered crowns, branches, roots, shrubs, rocks, haze, and a visible approach path. Physical-headset composition/performance verification remains required. |
| R31 | Ensure every website-affecting change is pushed and delivered to the live website | Process requirement | Website work is not complete until the validated source is committed and pushed to GitHub, the exact revision is published to the configured GitHub Pages site, and the affected public route is verified. A local build or `main` push alone is insufficient while Pages uses the generated `gh-pages` branch. Blocked delivery must be reported as deployment pending. |
| R32 | Make surround/spatial sound the automatic immersive VR/MR default, without an extra setting or persistent background audio | Implemented; device lifecycle smoke passed | The trusted local immersive action dispatches WebXR and Web Audio before awaiting either, then attaches the scene-bound HRTF engine to the XR camera. There is no audio toggle. Entry failure and XR session exit dispose the graph, while non-immersive 2D remains silent. Remote requests still require the headset's local **Confirm** action and cannot activate either protected API silently. Public source `959bf88` produced a Quest Browser audio player only during the observed immersive session; acoustic/perceptual validation remains required. |
